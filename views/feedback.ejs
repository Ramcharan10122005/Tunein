<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Feedback - Tune In</title>
    <script src="https://unpkg.com/lucide@latest"></script>
    <link href="https://fonts.googleapis.com/css2?family=Pacifico&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/main.css">
    <link rel="stylesheet" href="/responsive.css">
     <link rel="stylesheet" href="/responsive.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #121212;
            color: #eeecec;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        .container {
            max-width: 800px;
            margin: 40px auto;
            padding: 20px;
            flex: 1;
        }

        h2 {
            color: #eeecec;
            font-family: 'Pacifico', cursive;
            margin-bottom: 30px;
            font-size: 2.2rem;
            text-align: center;
            position: relative;
            padding-bottom: 15px;
        }

        h2:after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 50%;
            transform: translateX(-50%);
            width: 80px;
            height: 3px;
            background: #b62020;
            border-radius: 3px;
        }

        form {
            background: linear-gradient(145deg, #1a1a1a, #2a2a2a);
            padding: 40px;
            border-radius: 16px;
            display: flex;
            flex-direction: column;
            gap: 20px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.3);
            border: 1px solid rgba(255, 255, 255, 0.05);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        form:hover {
            transform: translateY(-5px);
            box-shadow: 0 12px 25px rgba(0, 0, 0, 0.4);
        }

        .input-group {
            position: relative;
            display: flex;
            align-items: center;
            margin-bottom: 5px;
        }

        .input-group i {
            position: absolute;
            left: 15px;
            color: #b62020;
            width: 22px;
            height: 22px;
            stroke-width: 1.5;
            transition: transform 0.3s ease;
        }

        .input-group:focus-within i {
            transform: scale(1.1);
        }

        input[type="text"],
        input[type="email"],
        textarea {
            padding: 15px 15px 15px 50px;
            font-size: 16px;
            border: 1px solid #404040;
            border-radius: 8px;
            background: rgba(64, 64, 64, 0.5);
            color: #eeecec;
            transition: all 0.3s ease;
            width: 100%;
            box-sizing: border-box;
            backdrop-filter: blur(5px);
        }

        textarea {
            min-height: 180px;
            resize: vertical;
            padding-top: 15px;
            line-height: 1.5;
        }

        input[type="text"]:focus,
        input[type="email"]:focus,
        textarea:focus {
            outline: none;
            border-color: #b62020;
            background: rgba(64, 64, 64, 0.7);
            box-shadow: 0 0 0 3px rgba(182, 32, 32, 0.2);
        }

        input[type="text"]::placeholder,
        input[type="email"]::placeholder,
        textarea::placeholder {
            color: #aaa;
            opacity: 0.7;
        }

        button[type="submit"] {
            padding: 15px;
            font-size: 18px;
            font-weight: bold;
            background: linear-gradient(45deg, #b62020, #d42a2a);
            color: white;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
            margin-top: 10px;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        button[type="submit"]:hover {
            background: linear-gradient(45deg, #d42a2a, #e63939);
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(182, 32, 32, 0.4);
        }

        button[type="submit"]:active {
            transform: translateY(0);
        }

        button[type="submit"] i {
            width: 22px;
            height: 22px;
            stroke-width: 1.5;
        }

        .back-link {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            color: #b62020;
            text-decoration: none;
            margin-bottom: 30px;
            transition: all 0.3s ease;
            font-weight: 500;
            padding: 8px 16px;
            border-radius: 8px;
            background: rgba(182, 32, 32, 0.1);
        }

        .back-link:hover {
            color: #d42a2a;
            background: rgba(182, 32, 32, 0.2);
            transform: translateX(-5px);
        }

        .back-link i {
            width: 20px;
            height: 20px;
            stroke-width: 1.5;
        }

        .success-message {
            background: rgba(40, 167, 69, 0.15);
            color: #28a745;
            padding: 20px;
            border-radius: 8px;
            margin-bottom: 30px;
            display: flex;
            align-items: center;
            gap: 12px;
            border: 1px solid rgba(40, 167, 69, 0.3);
            animation: fadeIn 0.5s ease;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(-10px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .success-message i {
            width: 24px;
            height: 24px;
            stroke-width: 1.5;
        }

        .form-header {
            text-align: center;
            margin-bottom: 30px;
        }

        .form-header p {
            color: #aaa;
            margin-top: 10px;
            line-height: 1.6;
        }

        .form-footer {
            text-align: center;
            margin-top: 30px;
            color: #aaa;
            font-size: 14px;
        }

        /* Responsive Design */
        @media (max-width: 1024px) {
            .container {
                max-width: 90%;
                margin: 30px auto;
                padding: 15px;
            }
            
            h2 {
                font-size: 1.9rem;
                margin-bottom: 25px;
            }
            
            form {
                padding: 30px;
                gap: 15px;
            }
            
            input[type="text"],
            input[type="email"],
            textarea {
                padding: 12px 12px 12px 45px;
                font-size: 15px;
            }
            
            textarea {
                min-height: 150px;
            }
            
            button[type="submit"] {
                padding: 12px;
                font-size: 16px;
            }
        }
        
        @media (max-width: 768px) {
            .container {
                max-width: 95%;
                margin: 20px auto;
                padding: 10px;
            }
            
            h2 {
                font-size: 1.7rem;
                margin-bottom: 20px;
            }
            
            form {
                padding: 25px;
                gap: 12px;
            }
            
            .input-group i {
                left: 12px;
                width: 20px;
                height: 20px;
            }
            
            input[type="text"],
            input[type="email"],
            textarea {
                padding: 10px 10px 10px 40px;
                font-size: 14px;
            }
            
            textarea {
                min-height: 120px;
            }
            
            button[type="submit"] {
                padding: 10px;
                font-size: 15px;
            }
            
            .back-link {
                padding: 6px 12px;
                font-size: 14px;
            }
        }
        
        @media (max-width: 480px) {
            .container {
                margin: 15px auto;
                padding: 8px;
            }
            
            h2 {
                font-size: 1.5rem;
                margin-bottom: 15px;
                padding-bottom: 10px;
            }
            
            h2:after {
                width: 60px;
                height: 2px;
            }
            
            form {
                padding: 20px;
                gap: 10px;
            }
            
            .input-group {
                margin-bottom: 3px;
            }
            
            .input-group i {
                left: 10px;
                width: 18px;
                height: 18px;
            }
            
            input[type="text"],
            input[type="email"],
            textarea {
                padding: 8px 8px 8px 35px;
                font-size: 13px;
            }
            
            textarea {
                min-height: 100px;
            }
            
            button[type="submit"] {
                padding: 8px;
                font-size: 14px;
            }
            
            .back-link {
                padding: 5px 10px;
                font-size: 13px;
                margin-bottom: 20px;
            }
            
            .success-message {
                padding: 15px;
                font-size: 13px;
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <a href="/profile" class="back-link">
            <i data-lucide="arrow-left"></i>
            Back to Profile
        </a>
        
        <div class="form-header">
            <h2>Send Feedback</h2>
            <p>We value your input! Help us improve Tune In by sharing your thoughts, suggestions, or reporting any issues you've encountered.</p>
        </div>
        
        <% if (locals.success) { %>
            <div class="success-message">
                <i data-lucide="check-circle"></i>
                <div>
                    <strong>Thank you for your feedback!</strong>
                    <p>We appreciate your input and will review it shortly.</p>
                </div>
            </div>
        <% } %>
        
        <form action="/submit-feedback" method="POST">
            <div class="input-group">
                <i data-lucide="user"></i>
                <input type="text" name="username" placeholder="Username" value="<%= locals.username || '' %>" required>
            </div>
            <div class="input-group">
                <i data-lucide="user-circle"></i>
                <input type="text" name="name" placeholder="Full Name" required>
            </div>
            <div class="input-group">
                <i data-lucide="mail"></i>
                <input type="email" name="email" placeholder="Email Address" required>
            </div>
            <div class="input-group">
                <i data-lucide="message-square"></i>
                <input type="text" name="subject" placeholder="Subject" required>
            </div>
            <div class="input-group">
                <i data-lucide="file-text"></i>
                <textarea name="message" placeholder="Your Message" required></textarea>
            </div>
            <button type="submit">
                <i data-lucide="send"></i>
                Submit Feedback
            </button>
        </form>
        
        <div class="form-footer">
            <p>Your feedback helps us improve Tune In for everyone. We typically respond within 24-48 hours.</p>
        </div>
    </div>

    <script>
        lucide.createIcons();
    </script>
</body>

</html> 